export * from "../provider";

/**
 * Root design system provider bridging theme tokens and overlays to the
 * application tree. Re-exports the unified Gluestack provider for color mode
 * and styling contexts.
 */
export { Provider as GluestackUIProvider } from "../provider";
