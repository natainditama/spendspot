/**
 * PostCSS pipeline configuration for SpendSpot mobile styling infrastructure.
 * Registers Tailwind CSS PostCSS plugin to process component stylesheets.
 */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
